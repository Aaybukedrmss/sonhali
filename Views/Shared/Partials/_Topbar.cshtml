@using dotnet_store.Models
@inject Microsoft.AspNetCore.Identity.SignInManager<AppUser> SignInManager
@inject Microsoft.AspNetCore.Identity.UserManager<AppUser> UserManager
@inject dotnet_store.Models.DataContext DataContext
<section class="top-bar">
            <div class="container">
                <div class="row gy-2 align-items-center">
                    <div class="col-lg-2 col-sm-4 col-4">
                        <div class="navbar-brand d-flex align-items-center" style=" border radius 10px">
                            <a asp-controller="HiddenAdmin" asp-action="Index" class="d-inline-block">
                                <img src="~/KirazCo.png?v=1" alt="KirazCo" class="img-fluid me-2 topbar-logo" />
                            </a>
                            <a asp-controller="Home" asp-action="Index" class="d-none d-md-inline topbar-brand-text text-decoration-none text-primary">KirazCo</a>
                        </div>
                    </div>
                   
                    <div class="col-lg-5 col-sm-8 col-8 order-lg-last">
                        <div class="d-flex justify-content-end align-items-center gap-2">

                            <a asp-controller="Favorite" asp-action="Index" class="btn btn-outline-primary">
                                <i class="fa fa-heart"></i> <span class="ms-1 d-none d-sm-inline-block">Listem</span>
                            </a>
                            @{
                                var cartCount = 0;
                                if (SignInManager.IsSignedIn(User))
                                {
                                    var cu = await UserManager.GetUserAsync(User);
                                    if (cu != null)
                                    {
                                        cartCount = DataContext.CartItems.Count(ci => ci.UserId == cu.Id);
                                    }
                                }
                            }
                            <a asp-controller="Cart" asp-action="Index" class="btn btn-outline-primary ms-1 d-flex align-items-center">
                                <i class="fa fa-shopping-cart"></i>
                                <span class="ms-1 d-none d-sm-inline-block">Sepetim</span>
                                @if (cartCount > 0)
                                {
                                    <span class="badge bg-danger ms-2">@cartCount</span>
                                }
                            </a>


                            @if (SignInManager.IsSignedIn(User))
                            {
                                var currentUser = await UserManager.GetUserAsync(User);
                                var displayName = string.IsNullOrWhiteSpace(currentUser?.AdSoyad) ? currentUser?.UserName : currentUser!.AdSoyad;
                                <span class="ms-2 me-1 d-none d-sm-inline-block color: #f7c7ba">Merhaba, @displayName</span>
                            }
                            else
                            {
                                <a asp-controller="Account" asp-action="Login" class="btn btn-outline-primary">Giriş</a>
                                <a asp-controller="Account" asp-action="Create" class="btn btn-outline-primary ms-1">Kayıt Ol</a>
                            }

                            <div class="dropdown ms-2">
                                <button class="btn btn-outline-primary d-flex align-items-center" id="menuToggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    <i class="fa-solid fa-bars"></i>
                                </button>
                                <ul class="dropdown-menu dropdown-menu-end">
                                    <li><a class="dropdown-item" asp-controller="Profile" asp-action="Index">Profil / Hesap Ayarları</a></li>
                                    <li><a class="dropdown-item" asp-controller="Orders" asp-action="Index">Siparişlerim</a></li>
                                    <li><a class="dropdown-item" asp-controller="Favorite" asp-action="Index">Favorilerim</a></li>
                                    <li><a class="dropdown-item" asp-controller="Help" asp-action="Index">Yardım/Şikayet/Öneri</a></li>
                                    @if (SignInManager.IsSignedIn(User))
                                    {
                                        var au = await UserManager.GetUserAsync(User);
                                        var isAdmin = au != null && await UserManager.IsInRoleAsync(au, "Admin");
                                        var isSupport = au != null && await UserManager.IsInRoleAsync(au, "Support");
                                        var isManager = au != null && await UserManager.IsInRoleAsync(au, "Manager");
                                        
                                        if (isAdmin || isSupport || isManager)
                                        {
                                            <li><hr class="dropdown-divider" /></li>
                                            
                                            @if (isSupport && !isAdmin)
                                            {
                                                <li class="dropdown-header">Destek Paneli</li>
                                                <li><a class="dropdown-item" href="/admin">Destek Ana Sayfa</a></li>
                                                <li><a class="dropdown-item" href="/admin/support">Destek Talepleri</a></li>
                                            }
                                            else
                                            {
                                                <li class="dropdown-header">Yönetim</li>
                                                <li><a class="dropdown-item" href="/admin">Yönetim Ana Sayfa</a></li>
                                                
                                                @if (isAdmin || isManager)
                                                {
                                                    <li><a class="dropdown-item" href="/admin/orders">Siparişler</a></li>
                                                }
                                                
                                                @if (isAdmin)
                                                {
                                                    <li><a class="dropdown-item" href="/admin/sliders">Slider Yönetimi</a></li>
                                                    <li><a class="dropdown-item" href="/admin/categories">Kategoriler</a></li>
                                                    <li><a class="dropdown-item" href="/admin/products">Ürünler</a></li>
                                                    <li><a class="dropdown-item" href="/admin/roles">Rol Yönetimi</a></li>
                                                    <li><a class="dropdown-item" href="/admin/settings">Site Ayarları</a></li>
                                                }
                                                
                                                @if (isAdmin || isSupport)
                                                {
                                                    <li><a class="dropdown-item" href="/admin/support">Destek Talepleri</a></li>
                                                }
                                            }
                                        }
                                    }
                                    @if (SignInManager.IsSignedIn(User))
                                    {
                                        <li>
                                            <form asp-controller="Account" asp-action="Logout" method="post" class="m-0 p-0">
                                                @Html.AntiForgeryToken()
                                                <button type="submit" class="dropdown-item">Çıkış Yap</button>
                                            </form>
                                        </li>
                                    }
                                    else
                                    {
                                        <li><a class="dropdown-item" asp-controller="Account" asp-action="Login">Giriş</a></li>
                                        <li><a class="dropdown-item" asp-controller="Account" asp-action="Create">Kayıt Ol</a></li>
                                    }
                                </ul>
                            </div>
                        </div>
                    </div>
                    <div class="col-lg-5">
                        <form method="get" action="@Url.Action("List","Urun")">
                            <div class="input-group">
                                <input type="text" name="q" value="@Context.Request.Query["q"]" placeholder="Ürün, yazar, yayınevi, marka..." class="form-control">
                                <button class="btn btn-primary" type="submit"><i class="fa fa-search"></i></button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </section>