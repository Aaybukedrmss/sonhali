@{
    ViewData["Title"] = "Şifre Sıfırlama Gönderildi";
}

<h2>Şifre Sıfırlama</h2>

<p>Eğer e-posta adresi doğruysa, şifre sıfırlama linkini aşağıda göreceksiniz.</p>

@if(ViewBag.ResetLink != null)
{
    <div class="alert alert-info">
        <strong>Link:</strong>
        <a href="@ViewBag.ResetLink">@ViewBag.ResetLink</a>
    </div>
}
else
{
    <div class="alert alert-info">Eğer e-posta sistemimizde kayıtlıysa bir bağlantı oluşturulmuştur.</div>
}

<a asp-action="Login" class="btn btn-secondary">Giriş sayfasına dön</a>


